# 모기 궤적 예측 대회 — 참고 논문 정리 및 모델 적용 아이디어

> 대회: 월간 데이콘 '모기 비행 궤적 예측 AI 경진대회'  
> 목표: 40ms 간격 11시점 관측 → 80ms 후 3D 좌표 예측  
> 작성일: 2026-05

---

## 논문 1. VECTOR — Velocity-Enhanced GRU for 3D UAV Trajectory Prediction

**출처:** arXiv:2410.23305 (2024) | Prince Sultan University  
**키워드:** GRU, UAV, 3D 궤적, 속도 기반 입력, 실시간 예측

### 핵심 주장

기존 모델들이 **위치(position)** 데이터만 입력으로 쓰는 데 반해,  
**속도(velocity)** 데이터를 입력으로 사용하면 일반화 성능이 크게 향상된다.

> "위치는 절대 좌표에 의존하므로 학습 분포 밖의 공간에서 실패한다.  
> 속도는 상대적이고 범위가 일정해서 어떤 공간에서도 일반화된다."

### 모델 구조

```
입력: 속도 시계열 [v_t-n, ..., v_t]  (20 time steps × 3축)
  ↓
GRU Encoder  (hidden_dim=64, 2 layers, Dropout)
  ↓
GRU Decoder
  ↓
FC Layer → 예측 속도/위치
```

### 정규화 전략 (2가지 제안)

| 방법 | 설명 | 장점 |
|------|------|------|
| **Whitening** | 공분산 행렬의 Cholesky 분해 `p_w = L⁻¹(p - μ)` | feature 간 상관관계 제거 |
| **Max L2-norm** | 최대 벡터 크기로 나누기 `‖p‖_max = max_i √(x²+y²+z²)` | 방향 왜곡 없이 스케일 정규화 |

### 실험 결과

- 24가지 GRU 구성 실험 (위치 vs 속도, Whitening vs MaxNorm)
- **속도 기반 + Whitening 조합이 최고 성능**
- MSE 범위: `2×10⁻⁸ ~ 2×10⁻⁷` (실제 드론 실험)
- GRU가 LSTM보다 계산 효율 우수하면서 정확도 유사 또는 우위

### 우리 대회에 쓸 수 있는 것

- [ ] **입력을 위치 → 속도로 변환**해서 GRU에 넣기
- [ ] Whitening 정규화 적용 (`StandardScaler` 대체)
- [ ] Max L2-norm 정규화를 위치/속도 각각 실험

---

## 논문 2. IaKNN — Interaction-aware Kalman Neural Networks

**출처:** arXiv:1902.10928 (2019) | NTU, WeBank  
**키워드:** Kalman Filter, LSTM, 가속도 예측, Physics+딥러닝 결합

### 핵심 주장

물리 기반 모델(Kalman)과 딥러닝 모델을 **3개 층으로 분리해서 계층적으로 결합**하면  
각각의 장점을 모두 살릴 수 있다.

### 모델 구조 (3-Layer Architecture)

```
과거 궤적 시계열
  ↓
┌─────────────────────────────────┐
│  Interaction Layer (CNN+LSTM)   │  ← 가속도 패턴 추출
│  : 과거 궤적 → 상호작용 가속도   │
└─────────────────────────────────┘
  ↓
┌─────────────────────────────────┐
│  Motion Layer (Kinematics)      │  ← 물리 방정식
│  p_t = p_{t-1} + v·Δt + ½a·Δt² │
└─────────────────────────────────┘
  ↓
┌─────────────────────────────────┐
│  Filter Layer (Kalman + LSTM)   │  ← 노이즈 학습
│  Q_t, R_t 를 LSTM으로 학습       │
└─────────────────────────────────┘
  ↓
최종 궤적 예측
```

### 핵심 아이디어: 시변(time-varying) 노이즈 학습

기존 Kalman Filter는 Q(process noise), R(measurement noise)를 **고정값**으로 설정하지만,  
IaKNN은 이 값들을 **LSTM으로 동적으로 학습**한다.

```
Q_t = LSTM_Q(S⁻_{t0:t})    # 상태 기반 프로세스 노이즈
R_t = LSTM_R(T_{t0:t})     # 궤적 기반 측정 노이즈
```

### 실험 결과

- NGSIM 도로 데이터셋 (100,000 프레임)
- CV, V-LSTM, S-LSTM, CS-LSTM 대비 약 **10% RMSE 개선**
- Kalman Filter 없는 버전(IaKNN-NoFL) 대비 필터 추가 시 안정성 향상

### 우리 대회에 쓸 수 있는 것

- [ ] **Kalman의 Q, R을 고정값 대신 GRU/LSTM으로 학습**하는 방식 시도
- [ ] Motion Layer의 Taylor 전개식 `p_t = p_{t-1} + v·Δt + ½a·Δt²` → 가속도 피처로 활용
- [ ] 현재 구조 (Kalman → GRU 잔차 보정)의 이론적 근거 확인됨 ✅

---

## 논문 3. WTFTP — Wavelet Transform Flight Trajectory Prediction

**출처:** Nature Communications 14:5258 (2023) | Sichuan University  
**키워드:** Wavelet Transform, Encoder-Decoder, 시간-주파수 분석, 급기동 예측

### 핵심 주장

궤적 데이터는 **저주파(전체 추세)** 와 **고주파(급격한 방향전환)** 가 섞여 있는데,  
기존 딥러닝 모델은 시간 도메인만 보므로 급기동 구간에서 성능이 떨어진다.  
→ **Wavelet 변환으로 주파수 분리** 후 각각 학습하면 정확도가 올라간다.

### 모델 구조

```
입력: 과거 궤적 시계열
  ↓
Input Embedding Network (FC × 2, ReLU)
  ↓
LSTM Encoder → 잠재 표현 H
  ↓
  ┌────────────────────────────────────────┐
  │  DWT (Discrete Wavelet Transform)      │
  │  WTC0: 저주파 (전체 추세)               │
  │  WTC1: 중간주파 (방향전환)              │
  │  WTC2+: 고주파 (노이즈/급격한 변화)     │
  └────────────────────────────────────────┘
  ↓
Decoder_i (각 주파수 대역별)
  + Wavelet Attention Module
  ↓
IDWT (Inverse Wavelet) → 예측 좌표 복원
```

### Wavelet Attention Module

각 주파수 대역마다 **다른 Attention**을 적용해서 스케일별 패턴 학습:

```
Decoder_i 출력 → LayerNorm → FC → LayerNorm → FC → WTC_i
```

### 실험 결과

| 방법 | MAE (상대 개선) |
|------|---------------|
| Vanilla LSTM | baseline |
| TCN | 유사 |
| CNN-LSTM | 소폭 개선 |
| Transformer | 소폭 개선 |
| **WTFTP (제안)** | **최고 성능** |

- 특히 **상승/하강, 선회 등 급기동 구간**에서 압도적 우위
- WTC0(저주파) + WTC1(중간주파) 조합이 가장 기여도 높음

### 우리 대회에 쓸 수 있는 것

- [ ] **Wavelet 피처 추가**: 11시점 궤적에 DWT 적용 후 저주파/고주파 계수를 GRU 입력으로 추가
- [ ] 급격한 방향전환(모기의 jerk) 구간만 따로 분류 후 다른 모델 적용
- [ ] `pywt` 라이브러리로 간단하게 구현 가능

---

## 종합 비교표

| 논문 | 핵심 기법 | 우리 대회 적용 난이도 | 기대 효과 |
|------|----------|-------------------|----------|
| VECTOR | 속도 입력 + Whitening | ⭐ (쉬움) | +0.01~0.02 |
| IaKNN | 시변 Kalman 노이즈 학습 | ⭐⭐ (중간) | +0.005~0.01 |
| WTFTP | Wavelet 주파수 분해 | ⭐⭐⭐ (어려움) | +0.01~0.02 |

---

## 모델 개선 로드맵 (우선순위 순)

### Step 1 — 빠른 개선 (1~2일)

```python
# VECTOR 아이디어: 입력을 속도로 변환
v = np.diff(X, axis=1) / DT          # (N, 10, 3)
a = np.diff(v, axis=1) / DT          # (N,  9, 3)

# Whitening 정규화 (Cholesky 기반)
cov = np.cov(v.reshape(-1, 3).T)
L = np.linalg.cholesky(cov)
v_white = (v - v.mean(axis=(0,1))) @ np.linalg.inv(L).T
```

**기대 효과:** 0.67 → 0.68+

### Step 2 — 중간 개선 (2~3일)

```python
# IaKNN 아이디어: Kalman 노이즈를 GRU로 학습
class AdaptiveKalman(nn.Module):
    def __init__(self):
        self.gru_Q = nn.GRU(3, 16)    # process noise 예측
        self.gru_R = nn.GRU(3, 16)    # measurement noise 예측
        self.fc_Q = nn.Linear(16, 1)
        self.fc_R = nn.Linear(16, 1)
    
    def forward(self, traj):
        Q = torch.exp(self.fc_Q(self.gru_Q(traj)[0]))
        R = torch.exp(self.fc_R(self.gru_R(traj)[0]))
        return Q, R                    # 동적 노이즈
```

**기대 효과:** +0.005~0.01

### Step 3 — 고급 개선 (3~5일)

```python
# WTFTP 아이디어: Wavelet 피처 추가
import pywt

def wavelet_features(traj, wavelet='db4', level=2):
    feats = []
    for axis in range(3):
        coeffs = pywt.wavedec(traj[:, axis], wavelet, level=level)
        feats.extend([c.mean(), c.std(), np.abs(c).max() for c in coeffs])
    return np.array(feats)             # 저주파/고주파 계수를 스칼라 피처로
```

**기대 효과:** +0.01~0.02

### Step 4 — 앙상블 (최종)

```
GRU_A (속도 입력, Whitening)
  +
GRU_B (위치 입력, 현재 구조)
  +
Transformer (별도 학습)
  ↓
가중 평균 앙상블
  ↓
Per-axis Calibration
```

**기대 효과:** 0.69~0.70 목표

---

## 핵심 인사이트 요약

1. **위치보다 속도가 일반화에 유리** (VECTOR): 센서 좌표계가 달라도 속도 패턴은 유사
2. **Kalman + 딥러닝 = 검증된 조합** (IaKNN): 물리 기반 + 데이터 기반의 시너지
3. **급방향전환 구간이 병목** (WTFTP): 모기의 jerk motion 예측이 핵심 난제
4. **노이즈 추정을 학습하라** (IaKNN): 고정 Q/R 대신 동적 학습이 효과적
